# Merge graph fixtures

Minimal schema `6.0.0` artifacts for `grapheinstein merge` tests.

- `a.json` / `b.json` — disjoint node ids (union succeeds)
- `conflict_a.json` / `conflict_b.json` — same node id, incompatible `type`
